-  name: Checkout and build Application
   hosts: all
   
   tasks:
   - name: Checkout 
     become: yes
     git:
        repo: '{{gitscheme}}://{{gituser}}:{{gitpass}}@{{giturl}}'
        dest: "{{ destdir }}"